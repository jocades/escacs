// export function debounce(cb: (...args: any[]) => any, ms: number) {
//   $effect(() => {
//     let timeout: number | null = setTimeout(async () => await cb(), ms)
//     return () => {
//       if (timeout !== null) {
//         clearTimeout(timeout)
//         timeout = null
//       }
//     }
//   })
// }
